# Free-Train 领域上下文

Free-Train 是一个 Windows 本地离线视觉数据预处理工具，用于把视频和图片转换为可审核、可追溯、可送入标注流程的未标注图片。首版不负责标注、数据集划分、模型训练或推理。

## 统一语言

**源素材（Source Asset）**：
用户导入且在整个处理过程中保持不变的视频或图片文件。
_避免使用_：原始数据、输入文件

**离线素材（Offline Source）**：
原地引用的源素材暂时不可访问，或当前文件与项目记录的内容指纹不一致。
_避免使用_：已删除素材，除非能够确认文件确实被删除

**来源组（Source Group）**：
由用户定义、可共享处理预设或参与跨来源相似比较的一组源素材。
_避免使用_：文件夹，除非该组确实由文件夹自动生成

**来源标识（Source Identifier）**：
用于命名和追溯的摄像头或来源名称，默认从相对文件夹生成，也可通过映射表覆盖。
_避免使用_：文件夹名，若用户已经完成映射

**处理任务（Processing Job）**：
对一个或多个源素材执行一份固定参数快照的可恢复运行实例。
_避免使用_：批次、流程

**处理流水线（Processing Pipeline）**：
把源素材转换为预处理成品的有序处理阶段集合。
_避免使用_：过滤链

**候选图片（Candidate Image）**：
由视频抽帧或图片导入产生、等待裁切、质检、相似筛选或人工审核的图片。
_避免使用_：抽帧图片，因为候选图片也可能来自静态图片

**预处理成品（Prepared Image）**：
经过用户确认并导出、准备进入标注流程的未标注图片。
_避免使用_：数据集图片、训练样本、最终帧

**来源追溯记录（Provenance Record）**：
把预处理成品关联回源素材、时间戳、ROI、切片坐标、处理参数和增强变体的元数据。
_避免使用_：日志

**感兴趣区域（ROI）**：
在源素材中标记有用画面的命名矩形区域。
_避免使用_：覆盖率、裁切框

**切片（Tile）**：
按照固定宽高从 ROI 中划分出的候选图片。
_避免使用_：Patch、Slice、裁切图

**切片位置（Tile Position）**：
标识切片在 ROI 中来源位置的稳定行列编号和源坐标。
_避免使用_：图片序号

**切片重叠率（Tile Overlap）**：
相邻切片在横向和纵向的交叠量，以像素或百分比表示。
_避免使用_：覆盖率

**画面变化触发抽帧（Change-Triggered Sampling）**：
根据指定分析区域的客观画面变化分数和时间间隔约束选择视频时间点的抽帧方式。
_避免使用_：AI 事件检测、场景理解

**人工抽帧（Manual Capture）**：
用户在视频审核播放器中选择精确解码时间点产生的候选图片。
_避免使用_：截图、自动抽帧

**有效片段（Valid Range）**：
用户标记的有用视频时间区间，可在区间内按规则批量抽帧。
_避免使用_：剪辑视频、子视频文件

**锁定候选图（Pinned Candidate）**：
默认保持可导出状态，只有用户显式解锁或排除才会改变状态的候选图片。
_避免使用_：无质量问题图片、不可修改图片

**质量评估（Quality Assessment）**：
在特定算法与阈值版本下记录的客观图片测量值和质量警告。
_避免使用_：内容价值评分

**视觉近重复图（Visual Near-Duplicate）**：
在配置的 pHash 和 SSIM 阈值下，与另一候选图片视觉结构高度接近的图片。
_避免使用_：语义相似图片

**相似组（Similarity Group）**：
在明确比较范围内发现的一组可审核视觉近重复候选图片。
_避免使用_：重复图片对、删除列表

**代表图（Representative Image）**：
根据确定性质量评分，在相似组内建议保留的候选图片。
_避免使用_：原图、主图

**增强配方（Augmentation Recipe）**：
包含增强操作、概率、参数范围和随机种子的可复现配置，仅在增强导出时生成图片。
_避免使用_：增强图片设置

**增强执行器（Augmentation Provider）**：
负责校验、预览和执行增强配方的版本化引擎。
_避免使用_：增强配方本身

**拍摄时间（Capture Time）**：
源素材最可信的绝对拍摄时间，并同时记录该时间的证据来源。
_避免使用_：视频内偏移、处理时间

**视频内偏移（Video Offset）**：
候选图片在源视频内的相对时间戳。
_避免使用_：拍摄时间、抽帧执行时间

## 关系

- 一个**处理任务**包含一个或多个**源素材**。
- 一个**源素材**可以暂时成为**离线素材**，但项目配置和历史不丢失。
- 一个**源素材**产生零个或多个**候选图片**。
- 一个**候选图片**产生零个或多个**预处理成品**。
- 每个**预处理成品**只有一条完整的**来源追溯记录**。
- 一个**源素材**可配置一个或多个命名**感兴趣区域**。
- 一个**感兴趣区域**按切片宽高、**切片重叠率**和边缘策略产生多个**切片**。
- 每个**切片**在对应 ROI 与切图配置中具有唯一的**切片位置**。
- 一个视频源素材可以包含多个**人工抽帧**和**有效片段**。
- **人工抽帧**产生的候选图片默认成为**锁定候选图**。
- 每个非空**相似组**都有一个建议**代表图**，用户可以更换。
- 一个**增强配方**由一个兼容的**增强执行器**执行。
- 同一候选图片及其增强变体共享一个血缘组标识。

## 示例对话

> **开发者：**“相似组建议排除一张人工抽帧图片时，需要把它删掉吗？”
> **领域用户：**“不需要。人工抽帧默认锁定，只显示相似和质量信息。只有我显式解锁并排除后，它才不参与导出，源视频始终不能被删除。”

## 已消除的歧义

- “训练数据”既可能指已标注样本，也可能指待标注图片。首版只产生未标注的预处理成品和追溯数据。
- “覆盖率”可能指 ROI 覆盖范围，也可能指切片交叠。统一使用“感兴趣区域”和“切片重叠率”。
- “相似”可能指视觉近重复或语义相同。首版相似筛选只处理视觉近重复，不使用 CLIP、DINO 等语义模型。
- “按时间命名”可能指拍摄时间、视频内偏移或处理时间。三者必须作为独立模板字段。
- “导入素材”不代表复制文件。首版默认原地引用，并通过内容指纹支持重新定位。
- “一键处理”不代表无人审核导出。快速处理运行到审核阶段，最终导出必须由用户确认。
- “数据划分”不属于 Free-Train。工具不生成 train、validation、test 划分，只保存血缘信息供下游系统使用。

