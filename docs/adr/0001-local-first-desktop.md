# 采用本地优先的 Windows 桌面架构并预留 MCP 边界

Free-Train 首版采用单用户 Windows 桌面应用，直接读取和写入用户授权的本地目录，不依赖服务器、账号或媒体上传。这能避免大型或敏感训练素材上云，并保持预览和批处理响应。桌面 MVP 通过独立应用服务调用处理引擎，不直接开放 MCP Server；待业务流程和数据结构稳定后，MCP 适配器复用同一应用服务，为 AI Agent 提供受目录权限、用户确认和审计记录约束的操作能力。

