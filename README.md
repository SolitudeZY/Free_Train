# Free-Train

Free-Train 是一个面向计算机视觉数据准备的 Windows 本地离线预处理工具。它处理未标注的视频和图片，提供视频抽帧、人工筛查、ROI 与固定尺寸切图、客观质量筛选、视觉近重复审核、可选数据增强、自由命名模板和可追溯导出。

当前状态：M4 已接通质量测量、精确/视觉相似分组、代表图和人工审核的首个纵向流程。详见 [M4 实现报告](./docs/M4-REPORT.md)。

## 文档索引

- [领域语言](./CONTEXT.md)
- [产品需求文档](./docs/PRD.md)
- [软件架构设计](./docs/ARCHITECTURE.md)
- [界面与交互设计](./docs/UI-DESIGN.md)
- [实施计划](./docs/IMPLEMENTATION-PLAN.md)
- [测试计划](./docs/TEST-PLAN.md)
- [M0 技术验证报告](./docs/M0-REPORT.md)
- [M1 实现报告](./docs/M1-REPORT.md)
- [M2 实现报告](./docs/M2-REPORT.md)
- [M3 实现报告](./docs/M3-REPORT.md)
- [M4 实现报告](./docs/M4-REPORT.md)
- [MCP 接口预留设计](./docs/MCP-DESIGN.md)
- [架构决策记录](./docs/adr/)

## 已确认边界

- Windows 10/11 本地桌面工具，单用户、内部使用。
- 只处理标注前素材，不负责标注、数据划分、模型训练或推理。
- 源素材默认原地引用且永不被修改或删除。
- 快速处理运行到人工审核阶段，最终导出必须确认。
- 桌面 MVP 不开放 MCP Server，但从首版保留独立应用服务边界。

## 开发命令

本机需要 Rust stable、Node.js、Corepack 和 WebView2。pnpm 通过 Corepack 调用，不要求全局安装垫片。

```powershell
corepack pnpm install
corepack pnpm check
cargo test --workspace
corepack pnpm --dir apps/desktop tauri dev
```

M1 使用方式：

1. 从顶部项目菜单新建或打开一个 `.ftproj` 项目目录。
2. 点击“导入”选择文件/目录，或把视频、图片和目录拖入窗口。
3. 在左侧来源树选择素材，中间预览，右侧查看来源、媒体与指纹信息。
4. 素材移动或被替换后点击左侧刷新按钮；离线素材可通过“重新定位”恢复引用。
5. 大批量素材只在左栏分段渲染；可勾选任意来源，使用“全选”选择项目内全部来源，或使用“选中离线”批量选择离线/异常来源。文件夹前的复选框会选择该文件夹内全部素材，点击文件夹名可展开或收起列表。
6. “移除选中”只删除项目引用和项目缓存，不会删除磁盘上的原始图片或视频，移除后可重新导入。确认后会显示处理进度、缓存候选数量和完成提示。

M2 视频筛查：

1. 选择在线视频，等待实际帧时间轴加载完成。
2. 使用底部播放、逐帧、速度和时间跳转控件定位目标。
3. 点击相机按钮或按 `C` 保存当前帧；人工候选默认锁定。
4. 按 `I` 设置入点、按 `O` 设置出点，创建一个或多个有效片段。
5. 在右侧“抽帧”页选择固定时间、固定帧间隔、目标数量、有效片段或画面变化模式。
6. 先执行预估，再执行抽帧；候选图片显示在底部缩略图条。
7. 候选缩略图可独立勾选，并支持清空选中或清空当前视频的全部候选；画面变化曲线可放大查看。

M3 ROI 与基础导出：

1. 选择静态图片或一个视频候选帧，进入“处理”中的“ROI / 切图”。
2. 在画布拖拽新建 ROI，或使用 X、Y、宽、高输入精确调整。
3. 选择来源组预设或当前来源覆盖，配置切片尺寸、重叠率、边缘与填充策略。
4. ROI 拖选和移动会实时显示边框，松开后自动保存；也可使用快捷键保存或保存并预览。
5. 点击“预览切片”后，画布网格和底部缩略图使用正式导出的同一坐标计划；图片与视频切片都可勾选并从本次导出中排除，重新生成预览可恢复。
6. 进入“导出”，选择原图/抽帧或 ROI 切片；视频可导出全部候选或当前候选帧，图片可按当前来源或整个来源组批量导出。
7. 先检查全量导出计划，再确认导出图片和 JSONL 来源清单；导出面板不会占用导航状态，可随时切换其他栏目。

M4 质量、相似与审核：

1. 点击顶部“快速处理”或左侧“审核”，配置最小尺寸、清晰度、曝光和低信息量阈值。
2. 选择同一来源、来源组或全项目相似比较范围，配置 pHash 距离、SSIM 阈值和视频时间窗口。
3. 运行分析后，在审核网格按状态、来源或相似组筛选；建议排除项使用浅白蒙层，代表图使用绿色边框。
4. 勾选审核项后执行保留、排除或恢复；锁定项优先成为代表图，多锁定冲突会显示警告。
5. 人工决定独立于自动建议，重新分析不会覆盖；正式导出跳过人工排除项，但不会仅凭自动建议跳过图片。

生成 release EXE：

```powershell
corepack pnpm --dir apps/desktop tauri build --no-bundle --ci
```

准备 LGPL FFmpeg 运行时：

```powershell
.\scripts\fetch-ffmpeg-lgpl.ps1
```
